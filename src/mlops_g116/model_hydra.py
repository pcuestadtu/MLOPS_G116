## Falta configurar hydra para model (hay que modificar config.yaml y poner variante model, pero para esto necesitariamos
# saber antes numero de capas etc o almenos tipo de modelo a usar). Tambien ponerlo como executable script en pyproject.toml.
